# Henry Reith

Personal website for Henry Reith.

Preview locally:

```sh
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

Resume:

The site links to `assets/henry-reith-resume.pdf`.
